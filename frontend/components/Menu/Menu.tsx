import * as React from "react";
import "./Menu.module.scss";

const Menu = (): JSX.Element => {
    return (
        <div className="Menu">
            <p>Menu</p>
        </div>
    );
}

export default Menu;